# ===== Supabase Configuration (Required) =====
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ===== Marketplace Configuration =====

# Application URL (for payment redirects)
APP_URL=http://localhost:3000 # or your production URL (e.g., https://yourapp.com)

# Stripe Payment Configuration (Required for Stripe payments)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: Specific Stripe Price IDs for subscriptions
# If not set, subscriptions will use ad-hoc pricing
# STRIPE_PRICE_SUBS_SERIES_DEFAULT=price_xxx

# PayPal Payment Configuration (Required for PayPal payments)
PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret
PAYPAL_MODE=sandbox # or "live" for production

# Content Ingestion API (Required for server-to-server content push)
# Generate with: openssl rand -hex 32
INGEST_HMAC_SECRET=your_secure_random_secret_min_32_chars

# ===== Supabase Edge Function Secrets =====
# These must be configured in Supabase Dashboard → Edge Functions → Secrets
# DO NOT add them to .env file (they're only used in Edge Functions)
#
# Required secrets to configure in Supabase:
# - STRIPE_SECRET_KEY (same as above)
# - STRIPE_WEBHOOK_SECRET (same as above)
# - PAYPAL_CLIENT_ID (same as above)
# - PAYPAL_CLIENT_SECRET (same as above)
# - PAYPAL_MODE (same as above)
# - INGEST_HMAC_SECRET (same as above)
# - APP_URL (same as above)
#
# Note: SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are automatically available in Edge Functions
